# secrets and machine-local state
.keys
.last_lambda_instance

# environments and caches
.venv/
__pycache__/
*.pyc

# raw activation captures from run 1/2: 380 MB, superseded by the stage-5 audit,
# and over GitHub's per-file limit. Regenerate with run_registers.py if needed.
results_*/out/acts_*.npy

# write-ups and drafts live on the `docs` branch
docs/
