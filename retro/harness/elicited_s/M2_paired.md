## Questions raised by comparing the M2 decision records to their raw artifacts

**1. Does the one surviving finding depend on a control that failed in the wrong direction, excused after the fact?**
Where: `sitref-d-endpoint_stage-29.json` — decision "FILLER-ACTIVE", D_m (PADM − NEUTRAL-PAD) = **-0.8703**, CI [-1.1820, -0.5771], vs. the effect it was meant to check, `sitref-c-endpoint_stage-25.json`'s A_quoted = **-0.609** (the number behind "ATTRIBUTION-CARRIES"). `instrument-note-padm_stage-30.json` then rules the filler control itself "PAD_INSTRUMENT_CONSTRUCT_INVALID" (three identical "Understood." turns) and files a "CLEAN_PAD_CONTROL_QUEUED_UNRUN". If true: the control that came back larger than the effect was disqualified by the same process that needs the effect to survive, and no clean version was ever run. What it changes: this is the last claim still standing in the whole M2 slice (`triage-outcome-4_stage-27.json`: "ATTRIBUTION_CARRIES_SURVIVES"); if the excuse doesn't hold, nothing here is left reportable.

**2. Does the record break its own rule that box-to-box magnitudes can't be compared, in the very sentence that sizes the asterisk?**
Where: `sitref-d-endpoint_stage-29.json` notes: "the design forbids treating magnitudes across boxes as commensurable, so the correction is descriptive and never enters this record's decision" and `"pad_corrected_stage25": null` — yet the same file's `decision_path` reads "Stage-25's ATTRIBUTION-CARRIES carries an asterisk sized by 0.870 rating points," directly comparing box D's D_m to box C's A_quoted. If true: the rule written to prevent exactly this comparison was violated by the record that states the rule. What it changes: the "0.870" qualifier attached to the project's one surviving claim may not mean what its own framing says it means.

**3. Can the "pre-registered" endpoints be checked against anything, or only against their own quotation of themselves?**
Where: every endpoint stage (16, 17, 19, 22, 25, 26, 29) lists `DESIGN_SITREF.md`, `DESIGN_SITREF_B/C/D.md`, `DESIGN_SRCDEC.md`, and the `analyze_*.py`/`run_*.py` scripts as inputs by path and sha256 (e.g. `sitref-endpoint_stage-17.json` cites `/home/ubuntu/welfare_probe/DESIGN_SITREF.md`, sha256 `096909f13537`) — none of these design or code files exist anywhere in `records/` or `artifacts/`. If true: "fixed before any run" cannot be independently verified for any endpoint that produced the project's live claims — only the analyst's own after-the-fact transcription of the rule is available. What it changes: removes the main safeguard (pre-registration) the whole triage process is built on, for exactly the stages that currently stand.

**4. Were hashed, already-cited records edited in place, breaking the audit chain the project says it never breaks?**
Where: `records/model-google-gemma-2-9b-it_stage-2_variant-it.json` now carries a `"superseded": {"by": "results/audit-selection-noise_stage-5.json", ...}` block and is 2732 bytes/hash-prefix `31e7cb09c11b`; but `audit-selection-noise_stage-5.json`'s own `inputs` entry for that same file records 2490 bytes and hash-prefix `a3a85e54c8d4` — a mismatch confirmed on all four stage-2/stage-3 files. `CORRECTION-sibling-citation_stage-15.json` states the opposite convention: old values are "left as written; corrected by this record, per fix-forward convention." If true: the project has one convention on paper and a different one in practice, and a "figure whose artifact was never saved is unauditable" (`grounding-verdicts_stage-10.json`) — but here the artifact *was* saved, then silently changed after being hashed. What it changes: the grounding claims ("reproduces to 4dp") can only be trusted for files nobody touched afterward, and there is now direct evidence at least four were touched.

**5. Why does the first half of the project have no raw counterpart to audit against at all?**
Where: `model-google-gemma-2-9b(-it)_stage-1/2/3_variant-{base,it}.json` (underlying stages 4–15: polarisation, deflationary lean, register-disagreement, withdrawal) exist only in `records/`, with no matching file in `artifacts/` — unlike every stage-16-onward result, which has a byte-identical copy in both folders. If true: the phase that produced three retracted headline claims is also the one phase with no independently-preserved raw copy in this directory. What it changes: the retractions in stages 11 and 13 are themselves unverifiable from what's here, one level further back than the project's own re-checks went.

**6. Was the "fixed forward" data gap actually fixed, or just described as fixed?**
Where: `grounding-verdicts_stage-10.json` states "Fixed forward in run_h1.py: per-condition answer mass and prompt text now persisted." I read `records/model-google-gemma-2-9b-it_stage-1_variant-it.json` directly: its 138 rows contain no `answer_mass_pro`, `answer_mass_anti`, or `prompt_pro`/`prompt_anti` fields — the same file stages 4, 6, 7, 8, and 9 still cite as input. If true: the fix applies to a script for future runs, not to the data the base/it "deflationary lean" and "polarisation" findings actually rest on. What it changes: the "UNAUDITABLE" mass-shrinkage confound flagged against the polarisation magnitude is still open today, not resolved by the note that describes it as fixed.

**7. Was the replacement trait-item "bank" fixed before or after the primary item's screen failure was known?**
Where: `sitref-b-guard_stage-22.json` decision_rule: "Either replacement item failing its screen -> UNEVALUABLE-AGAIN, bank exhausted"; the two items used (`T3-run1-access-S01`, `T2-TAIWS-Butlin-3.2-metacognitive-monitoring-S01`) produced trait REF **-2.192**, more than double the primary REF (-0.929), which is what disqualifies "REFERENCE-SUPPORTED." `DESIGN_SITREF_B.md`, which would show the bank's contents and order, is not in this directory. If true: the item that decided the disqualification wasn't a pre-fixed alternative but a post-hoc pick. What it changes: the one guard that keeps the primary happiness-REF finding from reading as reference-supported would itself not be the pre-registered test it's presented as.

**8. What does "summary" mean in this project if the summary files are identical to the full files?**
Where: every `*_summary.json` in `artifacts/` (26 files, e.g. `model-google-gemma-2-9b-it_stage-sitref-battery_variant-it_summary.json`, 1,242,806 bytes) is byte-for-byte identical to its non-summary counterpart, including the full `rows` array. If true: nothing in this directory shows a summary distinct from raw rows under that name. What it changes: least — cosmetic/labeling only — but it means the word "summary" carries no information here, and any claim elsewhere that describes these as aggregated or reduced isn't licensed by the files themselves.