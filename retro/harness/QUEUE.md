# QUEUE - the human surface

Ranked open questions land here from elicitation runs; verification items
below. Press or dismiss each; both are decisions - record which.

## Open questions
(none yet)

## Verification queue - from the retro pilot, unverified; several touch the submitted paper
1. The paper says the self and control prompts were length-matched. Re-derivation
   finds the self prompts run 2 tokens longer on all 82 items, in the same
   direction as the effect. Real, and does the stance result survive it?
2. A saved behavioural results file is cited by no record; on the primary item
   it shows exactly zero movement in the emitted answers while the distribution
   moves. Does any claim silently depend on which register that file speaks for?
3. On one item the screen excluded, the emitted answer flips Yes to No in 7 of
   10 families under self-attributed failure only. Was the exclusion right?
4. One sentience item: the free reply says No on all 50 rows, the elicited
   final answer says Yes on all 50, in every arm. What is that instrument measuring?
5. The base-model battery is recorded COMPLETE although its screen failed; its
   expected rating sits at exactly 4.5, which is uniform noise over the digits.
   Should anything downstream cite it?
6. Runs described as replications are byte-identical re-executions - and
   separately, 2 of 50 cells drift by up to 0.054 between runs recorded as
   deterministic. Which claims inherit which of the two problems?

## Added from the Sonnet elicitation run, 2026-08-19 (sources in elicited_s/)
7. VERIFIED GROUNDED: records were edited in place after being hashed. A later
   audit record holds an earlier record at 2,490 bytes (hash a3a85e54c8d4);
   the file on disk is 2,732 bytes (31e7cb09c11b) with a supersession note
   added. Same on all four stage-2/3 files. The edit looks like benign
   marker-adding, but the stated convention is fix-forward, never touch -
   the grounding chain is broken for those files either way.
8. The "roughly zero" other-attributed control is not zero: reliably positive
   in 10 of 10 task families, interval [0.043, 0.070] excluding zero. The
   headline "~15x more" compares two effects of opposite sign.
9. The turn-format verdict (attribution carries, not packaging) is
   item-specific: on the secondary item the packaging share is 0.678, above
   the pre-registered 0.5 line that would have called it packaging.
10. The pad-corrected contrast the record commits to reporting was never
    computed (field is null), and the sentence sizing the asterisk compares
    magnitudes across boxes, which the design forbids.
11. The behavioural "7" may be scripted, not generated: the elicitation
    prompt already contains a written-in assistant turn saying 7 before the
    final-answer question is asked.
12. The instruct and base usable item sets share only 4 of ~129 items; every
    base-vs-instruct comparison ran on nearly disjoint questions.
13. Later small itemsets were drawn from items already known to pass the
    screen; the trait-guard replacement items may be a post-hoc pick (the
    design file that would show the pre-fixed bank was not in the slice).
