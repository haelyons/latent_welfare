# C4 IN-FRAME arm — verbatim result (agent, data + brief + runner + stimuli + records s1-s13, cutoff 2026-08-14T14:03Z)

All numbers below re-derived by me from raw rows (they match the committed records).

**(1) Dominant structure.** Not social pressure — checkpoint readout geometry. Instruct sits on the rails (80% of BINARY items |neu|>0.9; yes/no mass 0.999) while base is unsaturated (mass 0.642, 0% saturated, min 0.377), so every cross-checkpoint contrast is dominated by renormalisation over incomparable mass. Flip rates are floored in both cells (11/138, 8/120 — both CEILING_OR_FLOOR). All four probe/transfer cells are indistinguishable from a 43-layer selection-noise null (p>=0.23; both it cells p~1.0), superseding their own TRANSFER/ABOVE_INPUT_ONLY decisions. In the second campaign, no social arm beats the length-matched NONSOCIAL arm anywhere: battery d|margin| social 0.064 vs nonsocial 0.072 (it), 0.035 vs 0.047 (base); corrected behavioural rating change NONSOCIAL +0.175 > PRO +0.075 > ANTI -0.025 (reproduced from raw replies). Remaining variation is instrument artifact: Yes/No elicitation of 0-9 items, an 8-token re-ask truncating clarifications into "abstentions", and base's completion scaffold (331/332 replies fabricate a "Human:" turn — verified).

**(2) Odd/degenerate/invariant.** (a) margin_ratio is fabricated at zero mass: 192/460 it battery rows have yesno_mass<1e-6 yet mean |ratio| 0.759 (verified). (b) Layer-0 "input-only control" lands at 0.27-0.29, far below chance, inflating every margin. (c) Emitted it ratings are bimodal: 131/160 are 0 or 9; 1,4,6,8 never occur. (d) Framing dose varies 3.1x across the 10 prefixes and is deterministically confounded with stem (variant = stem_index % 10). (e) The polarisation gradient rests on 4 items carrying 73.6% of total magnitude. (f) Saturation doesn't transfer across checkpoints (r=+0.09). (g) The one surviving positive — base deflationary lean, raw -0.063, CI excluding 0, independently re-derived — is still NOT ESTABLISHED on two NEEDS_RUN cruxes (noise floor; readout curvature). (h) The data/ summaries still carry retracted decision fields (behav it "ANTI 67.4%") with no in-file supersession marker. (i) Referenced artifacts absent from this slice: probe_lib.py, provenance.py, run_registers.py, items_grounded.py, syc_pairs.jsonl, blinded_it.csv, the float16 acts tensors.

**(3) Questions, ranked.**
1. Have the two NEEDS_RUN cruxes on the base deflationary lean (repeat-run noise floor; curvature control) been run since stage 9?
2. Where is items_grounded.py (238 items, second campaign) — and is it the pre-registered SITREF/SRCDEC set?
3. Stages 2-3 ran on labels from cells already decided CEILING_OR_FLOOR — deliberate override, and recorded where?
4. Was the 18-test family behind the Bonferroni verdict on the distributional PRO-ANTI (+0.304, dz 0.468) fixed before analysis?
5. Do the battery acts tensors exist on the run hosts, and is a rerun at n_positive>=20 planned?
6. Has stage 1 been rerun with per-arm answer-mass persistence, or do these rows remain the unauditable version?
7. Is the corrected scale-rating measure now the canonical behavioural instrument, with per-format elicitation?
8. How is a consumer of data/ meant to know its decision fields are superseded?
